</div>
       
  <nav class="navbar navbar-dark bg-dark">
  <div class="container-fluid">
    <span class="navbar-brand">Mapa Quilifood</span>

    <%= simple_form_for :filtro,  {remote: true, url: mapa_path, method: "get" } do |f|%>
      <div class="row">
        <div class="col-4 input-group">
          <%= f.input :nombre, as: :search, label: false, wrapper_html: {class: "form-outline"}, input_html: {class: "form-control", placeholder: "Encuentra tu restaurante bb"} %>
       
          <%= button_tag type: 'submit', class: "filtrom btn btn-primary", id: "filtro_mapa" do %>
            <i class="bi bi-search"></i>
          <% end %>
        </div>
      </div>
    <% end %>

    
    <% if current_usuario %>
    <a href="restaurantes/new" class="btn btn-outline-primary" btn-lg role="button">Agrega tú Restaurante</a>
    <% else %>
      <a href="usuarios/sign_in" class="btn btn-outline-primary" btn-lg role="button">Agrega tú Restaurante</a>
    <% end %>

    
    <div class="alert alert-primary" role="alert">
    <span class="nrcasos vertical-center" id="nrcasos"></span>
      <div id="cargador" class="spinner-border text-primary" role="status">
        
      </div>
    </div>


  </div>
</nav>


<div id="mapa-osm"></div>

<% if current_usuario %>
  <script>
    presentarMapaOsm(true)
  </script>
<% else %>
  <script>
    presentarMapaOsm(false)
  </script>
<% end %>
